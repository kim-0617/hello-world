from math import fabs
from tkinter import *

root = Tk()
root.title("Nado GUI")
root.geometry("640x480+300+100") # 가로 x 세로 + x좌표 + y좌표
root.resizable(False,False) # x,y 값 변경 불가 (창크기 안변함)

root.mainloop()